---
'@joggr/fastify-prisma': patch
---

Emit a one-time `DeprecationWarning` at module load and add a README banner announcing the upcoming rename to the unscoped `fastify-prisma` package. The `@joggr` scope will be retired; future releases will publish under `fastify-prisma`.
